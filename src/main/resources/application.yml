spring:
  application:
    name: TodayLetter

  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/todayletter?serverTimezone=Asia/Seoul&characterEncoding=UTF-8}
    username: ${DB_USERNAME:todayletter_app}
    password: ${DB_PASSWORD:hook040101}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
